load("@rules_cc//cc:defs.bzl", "cc_binary")

cc_binary(
    name = "counting_iterator_example",
    srcs = ["counting_iterator_example.cpp"],
    deps = [
        "//:iterator",
        "@boost//config",
    ],
)
